import { Meteor } from 'meteor/meteor';

Meteor.methods({
    // testImage: function(URL: string) {
    //     let tester = new Image();
    //     tester.onload = Meteor.call('isImage',true)
    //     tester.onerror = Meteor.call('isImage',false)
    //     tester.src = URL;
    // },
    // isImage: function(flag: boolean) {
    //     console.log('flag',flag);
    // }
});